# CHANGELOG_WAXWING

## 2025-09-22
- Replaced `src/components/calendar/Calendar.jsx` with a unified calendar component that preserves visuals and fixes multi-select / drag-range behavior. Supports `selectionMode: "single" | "multi" | "range"` and `variant: "select" | "preview"`.
- Updated `src/pages/QuotePage.jsx` heading to **Plan your shoot** and increased spacing above the stepper. Ensured stepper alignment consistency.
- No intentional visual changes elsewhere. Functional parity maintained.
